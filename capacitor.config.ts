import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shuttlebuddy.app',
  appName: 'ShuttleBuddyFE',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
