Pre-requisites:
Nodejs installed https://nodejs.org/en/
Ionic CLI installed https://ionicframework.com/docs/intro/cli
IDE such as VS code or Webstorm

Steps to get running:
1:  Open a terminal with the location set to the directory of the project 
      (most likely the terminal inside your IDE of choice)
2:  Run "npm install" in the terminal
3:  With ionic CLI installed run - "ionic serve"
4: the browser should open with the site displaying the login screen on http://localhost:8100